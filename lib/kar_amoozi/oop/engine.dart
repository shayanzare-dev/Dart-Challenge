class Engine {
  // bool status = false;

  void start(bool status) {
    // status = true;
    print('engine is start');
  }

  void stop(bool status) {
    // status = false;
    print('engine is stop');
  }
}
